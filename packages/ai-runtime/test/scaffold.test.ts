// Wave 0 stub - filled in plan 04-02 (GEN-03: scaffold produces a valid 5-file
// bundle with no ANTHROPIC_API_KEY).
import { describe, it } from "vitest";

describe("scaffold", () => {
  it.todo("scaffold backend produces a complete 5-file bundle (04-02)");
});
