// Wave 0 stub - filled in Task 3 (selectGenerator branches + BUNDLE_KEYS shape).
import { describe, it } from "vitest";

describe("generator", () => {
  it.todo("selectGenerator + BUNDLE_KEYS contract (Task 3)");
});
