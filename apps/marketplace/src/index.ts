// @utter/marketplace - the marketplace + A2A agent-card discovery surface
// (MKT-01/02/03/04). It lists registered resources from the ResourceRegistry
// index, serves each resource's A2A agent card and ERC-8004 identity, and exposes
// the discovery API agent operators query before paying per call. It reuses
// @utter/chain (registry reads), @utter/x402-arc (payment), @utter/ai-runtime, and
// @utter/sandbox.
//
// This is the Wave 0 barrel: the feature waves append the listing + agent-card
// routes. Nothing is exported yet.
export {};
