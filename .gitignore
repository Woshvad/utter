# GSD planning docs — kept local-only
.planning/

# Secrets
.env
.env.local
.env.*.local

# Dependencies / build output
node_modules/
dist/
build/
out/
.cache/

# The build-stream UI lives at apps/studio/app/components/build/ — it is SOURCE,
# not build output. Negate the bare build/ rule for this path so it is tracked.
!apps/studio/app/components/build/
!apps/studio/app/components/build/**

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db
