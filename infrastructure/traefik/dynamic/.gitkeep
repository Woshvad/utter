# The deployer's traefik-config generator writes per-resource dynamic config here
# (one <slug>.yml per deployed resource, file provider). This directory is kept
# in git via this placeholder; the generated *.yml files are runtime artifacts.
